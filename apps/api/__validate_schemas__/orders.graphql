query OrdersQuery {
    orders {
        _id
        total
        status
    }
}

query OrderQuery {
    order(_id: "xx") {
        _id
        total
        status
    }
}

#mutation CreateOrderMutation {
#    createOrder(data: {
#
#    })
#}